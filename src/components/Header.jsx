import React from "react"
import Navbar from "./NavBar"

export default function Header() {
    return < nav className="nav">
        <a href="/" className="site-title">Chase Ostien</a>
        <Navbar />
    </nav>
}